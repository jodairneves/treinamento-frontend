# URL do backend para proxy do Vite (desenvolvimento)
VITE_API_TARGET=http://localhost:3000

# URL do backend para Nginx (producao/Coolify)
# No Coolify, defina como a URL interna do servico backend
# Exemplo: http://barbearia-backend:3000
API_URL=http://backend:3000
